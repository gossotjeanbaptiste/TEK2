[defaults]
# Ansible configuration for G-DOP-400 bootstrap project
inventory = ../my-inventory
host_key_checking = False
roles_path = ./roles
log_path = ./ansible.log

[ssh_connection]
ssh_args = -C -o ControlMaster=auto -o ControlPersist=60s
